package group.phorus.exception.bdd.other

/**
 * Deliberately shares its simple name with [group.phorus.exception.bdd.CreateGroup]. Group
 * identity is the class, not the name, so a clone pinned to one must not keep constraints
 * scoped to the other.
 */
interface CreateGroup
